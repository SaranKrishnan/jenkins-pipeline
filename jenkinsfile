pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh 'echo "Saran - Hello World - Hello to you"'
                sh '''
                    echo "Multiline shell steps works too - Ah yes"
                    ls -lah
                '''
            }
        }
    }
}
